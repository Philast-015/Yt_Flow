[project]
name = "yt-flow"
version = "0.1.0"
description = "A youtube clone with working videos."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "flask-cors>=6.0.5",
    "requests>=2.34.2",
    "yt-dlp>=2026.6.9",
]
